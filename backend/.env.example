# Server Configuration
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# JWT Secrets (REQUIRED - Generate strong random secrets)
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))" to generate
JWT_SECRET=your_super_secure_jwt_secret_here_minimum_32_characters
JWT_REFRESH_SECRET=your_super_secure_refresh_secret_here_minimum_32_characters
SESSION_SECRET=your_super_secure_session_secret_here_minimum_32_characters

# Google OAuth
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Microsoft OAuth
MICROSOFT_CLIENT_ID=your_microsoft_client_id_here
MICROSOFT_CLIENT_SECRET=your_microsoft_client_secret_here

# Database (if using MongoDB)
# MONGODB_URI=mongodb://localhost:27017/perfind

# SECURITY NOTES:
# - JWT_SECRET, JWT_REFRESH_SECRET, and SESSION_SECRET are REQUIRED for the application to start
# - Use strong, random secrets (minimum 32 characters)
# - Never commit actual secrets to version control
# - Copy this file to .env and replace with actual values
# - SESSION_SECRET is used for CSRF protection and session management