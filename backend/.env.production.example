# Production Environment Variables Template
# Copy this file to .env.production and update with actual values

# Server Configuration
PORT=5001
NODE_ENV=production

# Frontend URL (Update with your Netlify domain)
FRONTEND_URL=https://your-app-name.netlify.app

# Backend URL (Update with your Railway backend URL)
BACKEND_URL=https://your-app-name.railway.app

# Database - MongoDB Atlas
# Replace with your MongoDB Atlas connection string
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# JWT Secrets (REQUIRED - Generate strong random secrets)
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))" to generate
JWT_SECRET=your_production_jwt_secret_here_minimum_32_characters
JWT_REFRESH_SECRET=your_production_refresh_secret_here_minimum_32_characters
SESSION_SECRET=your_production_session_secret_here_minimum_32_characters

# OAuth Configuration (Optional - Use same values from development)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
MICROSCOT_CLIENT_ID=your_microsoft_client_id
MICROSOFT_CLIENT_SECRET=your_microsoft_client_secret

# DEPLOYMENT CHECKLIST:
# 1. Update FRONTEND_URL with your actual Netlify domain
# 2. Update BACKEND_URL with your actual Railway domain
# 3. Replace MONGO_URI with your MongoDB Atlas connection string
# 4. Generate strong, unique secrets for production
# 5. Configure OAuth redirect URIs in Google/Microsoft consoles
# 6. Never commit this file with actual secrets to version control